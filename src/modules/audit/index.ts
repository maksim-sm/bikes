/**
 * audit module — public entry point.
 *
 * Owns the append-only audit log. Everything this module offers to the rest
 * of the application is re-exported here.
 *
 * No business logic yet; see `src/modules/README.md`.
 */

export {};
